## 比较逻辑

1. 两侧存在复杂类型 转换为基本类型
2. 都是基本类的情况下，如果两侧都是字符串，那么比较字符串，除此以外都转换为数字比较
3. > = 为 < 取反

## 题目

```
console.log(null>-1);
两侧不都是字符串 按照数字 0 > -1 true
console.log(null>=-1);
先计算 null < -1 两侧不都是字符串 转换数字 0 < -1 false => true
console.log(null<1);
转换数字 0<1  true
console.log(null<=1);
计算 null > 1 转换数字 0 > 1 false -> true
console.log(null==0);
两侧存在null false
console.log(null===0)
false
console.log(null!=0);
true
console.log(null!==0);
true
console.log('2' > 10);
两侧不都是字符串 转换为数字 2>10false
console.log("a" > 10)
数字 NaN > 10 只要有NaN都是false
console.log("a" < 10)
false
console.log("a" == 10)
类型不同 转换为数字 NaN == 10 false

Symbol(1) -Symbol(2)
VM783:1 Uncaught TypeError: Cannot convert a Symbol value to a number
    at <anonymous>:1:11
(anonymous) @ VM783:1
Symbol(1) >Symbol(2)
VM791:1 Uncaught TypeError: Cannot convert a Symbol value to a number
    at <anonymous>:1:11

console.log('2' > '10');
都是数字 字符串比较 true '2'>'1'

console.log('a' > 'b')
字符串比较false

console.log(‘a’>’A’)
字符串比较 小写更大

console.log('abc' > 'b');
false

console.log('abc' > 'aad');
true


var a = [ 42 ];
var b = "043";
a < b; // false
转换基本类型 "42" <"043" false


var a ={b:42}
var b = {b:43}
a< b
"[object Object]" < "[object Object]" false
"[object Object]" > "[object Object]" false
a==b 地址不一样 false

a >=b true
a<=b true

```
