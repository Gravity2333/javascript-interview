### 类型判断符号

```
A instanceof B B.prototype 是不是在A的原型链上
A.isPrototypeOf(B) A 是否在B的原型链上

in 某个属性是否在吗某个对象上 查找原型链
for...in.. 对象所有可枚举属性 查找原型

Reflect.ownKeys() 某个对象上所有的自有属性 不查找原型
Object.keys() 对象所有的可枚举自有属性 不查找原型

注意 in的语义是在原型找
记忆2组
in  for...in
Reflect.ownKeys() Object.keys()
```
